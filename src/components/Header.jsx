const Header = () => {
  return (
    <header>
      <h1>React Counter</h1>
      <p>
        A small web application that performs a basic counter using React Hooks
        (useState), with the ability to increase and decrease the count.
      </p>
    </header>
  );
};

export default Header;
